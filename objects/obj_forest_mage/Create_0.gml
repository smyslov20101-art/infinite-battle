// Create Event - obj_forest_mage

// Сначала инициализируем героя (это установит state и другие переменные)
forest_mage_init(id);

// Затем устанавливаем hero_id и hero_class
hero_id = -1;
hero_class = "";